package com.runer.toumai.util;

/**
 * Created by szhua on 2017/9/6/006.
 * github:https://github.com/szhua
 * TouMaiNetApp
 * Note
 */

public class Note {

/*//处理条件列表
    private void handleListView(View contentView) {
        containerList =contentView.findViewById(R.id.container);
        RecyclerView recyclerView = (RecyclerView) contentView.findViewById(R.id.recycler_view);
        LinearLayoutManager manager = new LinearLayoutManager(getContext());
        manager.setOrientation(LinearLayoutManager.VERTICAL);
        recyclerView.setLayoutManager(manager);
        HomeCheckGoodsByStateAdapter adapter = new HomeCheckGoodsByStateAdapter(stateBeans);
        VerticalItemDecoration decoration = ItemDecorations.vertical(getContext())
                .type(0, R.drawable.item_decoration_shape).create();
        recyclerView.addItemDecoration(decoration);
        recyclerView.setAdapter(adapter);
        sureFootView = View.inflate(getContext(), R.layout.state_choose_foot, null);
        adapter.addFooterView(sureFootView);
        sureFootView.setOnClickListener(this);
        containerList.setOnClickListener(this);
    }*/

}
